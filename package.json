{
  "name": "bundlejs-api",
  "private": true,
  "packageManager": "pnpm@10.8.1",
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "core",
    "compress",
    "utils",
    "edge",
    "cloudflare"
  ],
  "scripts": {
    "dev": "pnpm --dir cloudflare dev",
    "deploy": "pnpm --dir cloudflare deploy",
    "deploy:preview": "pnpm --dir cloudflare deploy:preview",
    "dev:cloudflare": "pnpm run dev",
    "typecheck:cloudflare": "pnpm --dir cloudflare typecheck",
    "deploy:cloudflare": "pnpm run deploy"
  }
}